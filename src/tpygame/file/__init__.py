"""
This module provides functionality related to file management and whitelisting.

The module includes utilities for managing files through the `FileManager` and
handling whitelisted entities using the `WhiteList`. These components facilitate
performing file-related operations and validation processes based on a predefined
whitelist logic.
"""
from .fm import FileManager
from .whitelist import WhiteList
